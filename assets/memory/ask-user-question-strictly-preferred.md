---
name: ask-user-question strictly preferred
description: When user input is needed, the user prefers immediate AskUserQuestion choice prompts instead of pausing with manually written options in chat.
type: feedback
---
When user input is needed, use AskUserQuestion directly rather than stopping with typed-out option lists in a normal message.

**Why:** The user explicitly objected to me pausing and manually listing options instead of presenting an interactive choice prompt.

**How to apply:** If there is a branch point or confirmation needed, immediately use AskUserQuestion with concise options unless the user has already authorized a default path.
